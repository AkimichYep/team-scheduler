<!DOCTYPE html>
<html>
<head>
    <title><%= pageTitle %> - Team Scheduler</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- AI Chat Header Panel (Collapsible at Top) -->
    <%- include('chat-header', { userId: (typeof currentUser !== 'undefined' && currentUser?.id) ? currentUser.id : 1 }) %>

    <%- include('header', { activePage: activePage }) %>

    <div class="container">
        <%- body %>
    </div>
</body>
</html>

